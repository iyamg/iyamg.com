{
  "buildCommand": "curl -fsSL https://go.dev/dl/go1.23.3.linux-amd64.tar.gz | tar -xz -C /tmp && PATH=$PATH:/tmp/go/bin hugo --gc --baseURL https://$VERCEL_URL",
  "outputDirectory": "public",
  "framework": null,
  "build": {
    "env": {
      "HUGO_VERSION": "0.147.0"
    }
  }
}
